class User {
  final String uid;
  final String email;
  final String imgUrl;
  final String displayName;
  final DateTime lastSeen;

  User(this.uid, this.email, this.imgUrl, this.displayName, this.lastSeen);
}
